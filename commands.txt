1   python src/prepare_model.py	-- Builds models/simulation.idf — trims the example building to the 3-week run period and instruments it
2	python src/run_experiment.py --mode -- all	Runs baseline → rule-based → agent back to back (~6 min, almost all LLM time). Requires Ollama running with qwen2.5:3b-instruct pulled
3	python dashboard/build_report.py --	Rebuilds dashboard/report.html from the new results