Put the baseline `.idf` and matching `.epw` weather file here once
EnergyPlus is installed (see the main [README](../README.md)):

- `baseline.idf` — the unmodified example building model
- `weather.epw` — matching weather file
- any runtime-modified `.idf` produced by an AI-driven run (deliverable #2
  in the brief requires committing these alongside the baseline)
