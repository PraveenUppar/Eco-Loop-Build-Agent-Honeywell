# Python 3.13. EnergyPlus itself is a separate install (see README) -- the
# pyenergyplus package ships with it and is added to sys.path at runtime.
eppy==0.5.69
mcp==1.28.1
numpy==2.5.1
ollama==0.6.2
pandas==3.0.5
plotly==6.9.0
