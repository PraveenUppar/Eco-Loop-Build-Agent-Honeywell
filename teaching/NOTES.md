# Notes (working scratchpad for the teacher, not the learner)

- Learner vibe-coded this project; understands *that* it works, not always
  *why*. Treat every design choice as something to explain, not assume.
- No AI/ML background and no EnergyPlus/BMS background — define terms on
  first use, every lesson. Glossary in `reference/glossary.html` is the
  catch-all; link to it rather than re-defining everything inline every time.
- Numbers used throughout come from `results/summary.json` (3-week run) and
  `SUBMISSION.md` (endurance run) — verified against source, not copied from
  memory.
- Session request front-loaded all scope (10 topics) instead of the usual
  one-lesson-at-a-time flow. Built lessons 1-9 as a full set in one pass;
  saved the grill (item 10 in their list) for live conversation since that's
  inherently interactive and shouldn't be pre-scripted with the "answer"
  visible.
- Repo root (`c:\Users\pavii\Desktop\Eco-Loop-Build-Agent-Honeywell`) is an
  active project, not a blank workspace — teaching material lives under
  `./teaching/` so it doesn't clutter `src/`, `models/`, etc.
